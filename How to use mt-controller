in the meterthingy directory run:
sudo python mt-controller.py &

mt-controller will run in the background, and will change the meter needle position to match the value it finds in mt.setting,

echo 20 > mt.setting
